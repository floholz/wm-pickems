# Go
/wm-pickems
/tmp/

# PocketBase runtime data (DB, storage, logs)
/pb_data/

# Node / SvelteKit
frontend/node_modules/
frontend/.svelte-kit/
frontend/build/

# Embedded build output — generated by the frontend build, not committed.
# A placeholder index.html is kept so `go build` works without a build.
/internal/web/build/*
!/internal/web/build/index.html

# Env
.env
*.local

# Editor / OS
.DS_Store
